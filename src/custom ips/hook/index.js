import React from "react";
import $ from "jquery";
import { useLocation } from "react-router-dom";
//import ExtraMenu from './extra_menu.js';

const Hook = (props) =>{
  var dynamicContent = '';

  switch(props.type) {
    
  }

  return(
    dynamicContent
  )


}

export default Hook;
